{
  "name": "@patchwork/api",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts"
  },
  "dependencies": {
    "@patchwork/core": "workspace:*",
    "drizzle-orm": "^0.38",
    "hono": "^4.7",
    "resend": "^6.9.3",
    "ulid": "^2.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30",
    "@types/bun": "latest"
  }
}
